# Raspberry Pi dependencies
# Install with: pip3 install --break-system-packages -r requirements.txt
#
# picamera2 and python3-opencv are best installed via apt:
#   sudo apt install -y python3-picamera2 python3-opencv
#
# The rest via pip:
adafruit-circuitpython-pca9685>=3.4
adafruit-circuitpython-servokit>=1.3
numpy>=1.24
